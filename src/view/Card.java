package view;

import javax.swing.*;

public class Card extends JPanel {

    private model.Card model;

    public Card(model.Card model) {
        this.model = model;

        // TODO set appropriate icon based on model
    }
}
